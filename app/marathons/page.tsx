import {PRE_RACE_WINDOW_WEEKS} from "@/analytics/training-window"; import Link from "next/link"; import races from "@/data/marathons.json"; import {Marathon} from "@/lib/types"; export default function Page(){const m=races as Marathon[];return <><div className="eyebrow">Race archive</div><h1 style={{fontSize:72}}>MARATHONS</h1><p className="lede">Open any race to see the result, split, context, and—where source data exist—the standardized {PRE_RACE_WINDOW_WEEKS}-week training cycle.</p><div className="race-list">{m.map(r=><Link className="race-row" href={`/marathons/${r.id}`} key={r.id}><b>{r.year}</b><strong>{r.race}</strong><span>{r.finish}</span><span>{r.half||'—'}</span><span className="muted">{r.context}</span></Link>)}</div></>}