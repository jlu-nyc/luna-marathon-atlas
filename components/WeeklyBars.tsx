import {PRE_RACE_WINDOW_WEEKS} from "@/analytics/training-window"; import {Cycle} from "@/lib/types"; export default function WeeklyBars({cycle}:{cycle:Cycle}){const max=Math.max(...cycle.weekly.map(w=>w.miles),1);return <div className="chart"><div className="bars">{cycle.weekly.map(w=><div key={w.week} className="bar" data-label={`${w.week}: ${w.miles} mi`} style={{height:`${Math.max(4,w.miles/max*100)}%`}} />)}</div><p className="muted">Weekly mileage across the standardized {PRE_RACE_WINDOW_WEEKS}-week pre-race window. Hover a bar for details.</p></div>}